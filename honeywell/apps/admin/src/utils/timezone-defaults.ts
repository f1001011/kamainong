/**
 * @file 时区默认值常量
 * @description 独立文件，无其他依赖，避免循环引用
 * 与 packages/config DEFAULT_CONFIG.SYSTEM_TIMEZONE 保持一致
 */

export const DEFAULT_TIMEZONE = 'Africa/Casablanca';
export const DEFAULT_TIMEZONE_DISPLAY_NAME = 'توقيت المغرب (UTC+1)';
